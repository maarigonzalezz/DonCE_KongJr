package org.servidor.servidor.juego.entidades;

// Esta vara es nada mas para probar las entidades

public final class EntitiesSmoke {
    /*public static void main(String[] args) throws InterruptedException {
        Level lvl = new Level();
        CocodriloRojo  rojo = new CocodriloRojo(2, 16*12,  60f);
        CocodriloAzul  azul = new CocodriloAzul(0, 16*10,  80f);
        Fruta banana = new Fruta(4, 16*12, 100);

        float dt = 1f/30f;
        for (int i=0; i<60; i++) {
            rojo.update(lvl, dt);
            azul.update(lvl, dt);
            banana.update(lvl, dt);

            if (i%10==0) {
                System.out.printf("t=%.2f  Rojo(y=%.1f)  Azul(y=%.1f, vy=%.1f, est=%s)  Fruta(x=?,y=%.1f)%n",
                        i*dt, rojo.y(), azul.y(), azul.vy(), azul.estado(), banana.y());
            }
            Thread.sleep(10);
        }
        System.out.println("Entities smoke OK.");
    }*/
}
